/*
_jsx 文件夹的控件还没正式开发，当前只是作为一个示例


此文件用来汇总 _jsx 文件夹的控件，主要是方便引用，只需要引用这一个文件的就可以了
引用方式：import { Select, Input } from './_jsx/importShared'
*/

import Select from './select'
import Input from './input'

export { Select, Input }